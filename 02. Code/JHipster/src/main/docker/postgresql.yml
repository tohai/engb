version: '2'
services:
  dungnn-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/dungnn/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=dungnn
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
